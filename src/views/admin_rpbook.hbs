<link rel="stylesheet" href="/css/style.css">
<link rel="stylesheet" href="/css/header.css">
<link rel="stylesheet" href="/css/book.css">  

<section class="explore">
  <div class="container">
    <div class="explore-content">

      <div class="product-head">
        <h1>Reported books</h1>
        <div class="row row-cols-5">
          {{#each reportlists}}
          <div class=" col">
            <div class="single-explore-item">
              <div class="single-explore-img">
                <a href = "/admin/details/{{Book.id}}"><img src={{Book.imagePath}} /img/logoicon.png"></a>
              </div>
              <div class="single-explore-txt bg-theme-5">
                <h2><a href="/admin/details/{{Book.id}}">{{Book.title}}</a></h2>
                <p class="explore-rating-price">
                  <span class="explore-price-box">
                    price:
                    <span class="explore-price">${{Book.price}}</span>
                  </span>
                  <span class="explore-price-genne">
                    novel
                  </span>
                  <a  href="">{{Book.author}}</a>
                  </span>
                </p>
                <div class="text_report">
                <p>"{{content}}" - Reported by {{User.username}}</p>
                </div>
                <div class="action-buttons">
                  <button onclick="deleteBook('{{reportedBookId}}')" class="accept-btn">Ban</button>
                  <button onclick="deleteReport('{{id}}')" class="remove-btn">Remove</button>
              </div>
              </div>
            </div>
          </div>
          {{/each}}
        </div>
      </div>
    </div>
      {{!-- </div>
    </div> --}}
  </div>
</section><!--/.explore-->